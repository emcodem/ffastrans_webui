import { h } from 'preact';
import type { Uppy } from '@uppy/core';
import type { AsyncStore } from '@uppy/core/lib/Uppy.js';
import { type PickedItem } from './googlePicker.js';
export type GooglePickerViewProps = {
    uppy: Uppy<any, any>;
    clientId: string;
    onFilesPicked: (files: PickedItem[], accessToken: string) => void;
    storage: AsyncStore;
} & ({
    pickerType: 'drive';
    apiKey: string;
    appId: string;
} | {
    pickerType: 'photos';
    apiKey?: undefined;
    appId?: undefined;
});
export default function GooglePickerView({ uppy, clientId, onFilesPicked, pickerType, apiKey, appId, storage, }: GooglePickerViewProps): h.JSX.Element;
//# sourceMappingURL=GooglePickerView.d.ts.map