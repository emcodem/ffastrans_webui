import { ResolveDepOptions } from './options';
import { File, FileMap } from './file';
export declare class Loader {
    private options;
    constructor(options: ResolveDepOptions);
    loadEntry(workingDirectory: string, request: string, files?: FileMap, warnings?: never[]): {
        entry: File;
        files: FileMap;
        warnings: string[];
    };
    private load;
}
