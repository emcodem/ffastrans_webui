{
    "extends": "../tsconfig.json",
    "compilerOptions": {
        "composite": true,

         // ./src dirs are the only ones that emit compiled code
        "noEmit": false,

        "rootDir": ".",
        "outDir": "../dist",
    }
}
