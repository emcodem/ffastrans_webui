'use strict';

module.exports = function getPrefix() {
  var indentLength = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 1;
  var indentChars = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : '  ';

  var prefix = '';
  for (var i = 0; i < indentLength; i += 1) {
    prefix += indentChars;
  }

  return prefix;
};