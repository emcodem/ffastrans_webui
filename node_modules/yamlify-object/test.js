const yo = require('./src/index');

const arr = {
  array: [
    [],
    [],
    []
  ],
};

arr.array.push(arr);
arr.array.push(arr.array);
arr.circular = arr;

console.log(yo(arr, {
  indent: '  ',
  prefix: '',
}));
